<core:FragmentDefinition xmlns="sap.m" xmlns:l="sap.ui.layout" xmlns:f="sap.ui.layout.form" xmlns:core="sap.ui.core" class="sapUiSizeCompact" xmlns:tnt="sap.tnt"
    xmlns:c="zui3derp.control" xmlns:t="sap.ui.table">
    <Toolbar height="3rem" style="Clear">
        <Label text="Detailed BOM"/>
        <ToolbarSpacer />
        <!-- <Button icon="sap-icon://save" text="Save"/>
        <Button icon="sap-icon://add"/>
        <Button icon="sap-icon://delete"/>
        <Button icon="sap-icon://edit"/> -->
        <Button icon="sap-icon://excel-attachment" press="onExportExcel"  tooltip="Download to Excel">
            <customData>
            <core:CustomData key="TableName" value="bomDetailedTable"/>
            </customData>
            <layoutData>
                <ToolbarLayoutData shrinkable="true"/>
            </layoutData>
        </Button>
    </Toolbar>
    <c:DynamicTable id="bomDetailedTable" rows="{DataModel>/results}" class="sapUiSizeCompact" showColumnVisibilityMenu="true">
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="BOM Item"/>
            <t:template>
                <!-- <Input value=""/> -->
                <Text text="{DataModel>Bomitem}"/>
            </t:template>
        </t:Column>
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="BOM Item Type"/>
            <t:template>
                <!-- <Input value="{DataModel>Bomitmtyp}"/> -->
                <Text text="{DataModel>Bomitmtyp}"/>
            </t:template>
        </t:Column>
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="Style"/>
            <t:template>
                <!-- <Input value="{DataModel>Bomstyle}"/> -->
                <Text text="{DataModel>Bomstyle}"/>
            </t:template>
        </t:Column>
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="Style Version"/>
            <t:template>
                <!-- <Input value="{DataModel>Bomstylver}"/> -->
                <Text text="{DataModel>Bomstylver}"/>
            </t:template>
        </t:Column>
        <!-- <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="Style Description"/>
            <t:template>
                <Input value="{DataModel>Attribtyp}"/>
            </t:template>
        </t:Column> -->
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="Part Code"/>
            <t:template>
                <!-- <Input value="{DataModel>Partcd}"/> -->
                <Text text="{DataModel>Partcd}"/>
            </t:template>
        </t:Column>
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="Part Description"/>
            <t:template>
                <!-- <Input value="{DataModel>Partdesc}"/> -->
                <Text text="{DataModel>Partdesc}"/>
            </t:template>
        </t:Column>
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="Part Count"/>
            <t:template>
                <!-- <Input value="{DataModel>Partcnt}"/> -->
                <Text text="{DataModel>Partcnt}"/>
            </t:template>
        </t:Column>
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="Usage Class"/>
            <t:template>
                <!-- <Input value="{DataModel>Usgcls}"/> -->
                <Text text="{DataModel>Usgcls}"/>
            </t:template>
        </t:Column>
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="Customer Style"/>
            <t:template>
                <!-- <Input value="{DataModel>Custstyle}"/> -->
                <Text text="{DataModel>Custstyle}"/>
            </t:template>
        </t:Column>
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="Process"/>
            <t:template>
                <!-- <Input value="{DataModel>Processcd}"/> -->
                <Text text="{DataModel>Processcd}"/>
            </t:template>
        </t:Column>
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="Color"/>
            <t:template>
                <!-- <Input value="{DataModel>Color}"/> -->
                <Text text="{DataModel>Color}"/>
            </t:template>
        </t:Column>
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="Size"/>
            <t:template>
                <!-- <Input value="{DataModel>Sizes}"/> -->
                <Text text="{DataModel>Sizes}"/>
            </t:template>
        </t:Column>
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="Destination"/>
            <t:template>
                <!-- <Input value="{DataModel>Dest}"/> -->
                <Text text="{DataModel>Dest}"/>
            </t:template>
        </t:Column>
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="Mat. Type"/>
            <t:template>
                <!-- <Input value="{DataModel>Mattyp}"/> -->
                <Text text="{DataModel>Mattyp}"/>
            </t:template>
        </t:Column>
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="GMC"/>
            <t:template>
                <!-- <Input value="{DataModel>Gmc}"/> -->
                <Text text="{DataModel>Gmc}"/>
            </t:template>
        </t:Column>
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="Material"/>
            <t:template>
                <!-- <Input value="{DataModel>Matno}"/> -->
                <Text text="{DataModel>Matno}"/>
            </t:template>
        </t:Column>
        <!-- <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="GMC Desc"/>
            <t:template>
                <Input value="{DataModel>Attribtyp}"/>
            </t:template>
        </t:Column> -->
        <!-- <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="Addtl. Desc"/>
            <t:template>
                <Input value="{DataModel>Attribtyp}"/>
            </t:template>
        </t:Column> -->
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="Consump Per"/>
            <t:template>
                <!-- <Input value="{DataModel>Consump}"/> -->
                <Text text="{DataModel>Consump}"/>
            </t:template>
        </t:Column>
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="Per"/>
            <t:template>
                <!-- <Input value="{DataModel>Per}"/> -->
                <Text text="{DataModel>Per}"/>
            </t:template>
        </t:Column>
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="UOM"/>
            <t:template>
                <!-- <Input value="{DataModel>Entryuom}"/> -->
                <Text text="{DataModel>Entryuom}"/>
            </t:template>
        </t:Column>
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="Wastage"/>
            <t:template>
                <!-- <Input value="{DataModel>Wastage}"/> -->
                <Text text="{DataModel>Wastage}"/>
            </t:template>
        </t:Column>
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="Computed Consumption"/>
            <t:template>
                <!-- <Input value="{DataModel>Comconsump}"/> -->
                <Text text="{DataModel>Comconsump}"/>
            </t:template>
        </t:Column>
        <t:Column width="7rem" filterType="" filterProperty="Attribtyp" sortProperty="Attribtyp">
            <Label text="Consumption"/>
            <t:template>
                <!-- <Input value="{DataModel>Consump}"/> -->
                <Text text="{DataModel>Consump}"/>
            </t:template>
        </t:Column>
    </c:DynamicTable>
</core:FragmentDefinition>            