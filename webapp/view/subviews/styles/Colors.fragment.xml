<core:FragmentDefinition xmlns="sap.m" xmlns:l="sap.ui.layout" xmlns:f="sap.ui.layout.form" xmlns:core="sap.ui.core" class="sapUiSizeCompact" xmlns:tnt="sap.tnt" xmlns:c="zui3derp.control" xmlns:t="sap.ui.table">
    <!-- <Panel expandable="true" expanded="false" width="80%" class="sapUiSizeCompact">

        <headerToolbar>
            <Toolbar height="2rem" style="Clear">
                <Label text="{i18n>Colors}" />
            </Toolbar>
        </headerToolbar> -->
    <VBox fitContainer="true" width="80%" class="sapUiTinyMargin">
        <Toolbar height="2rem" style="Clear">
            <ToolbarSpacer />
            <Button id="btnAddColor" type="Ghost" icon="sap-icon://add" tooltip="{i18n>Addnewline}" press="addLine" visible="true">
                <customData>
                    <core:CustomData key="TableName" value="colorsTable" />
                </customData>
            </Button>
            <Button type="Ghost" icon="sap-icon://save" tooltip="{i18n>Savecolorattributes}" press="onSaveColorTable" visible="{= ${ColorEditModeModel>/editMode} === true ? true : false }" />
            <Button type="Ghost" icon="sap-icon://edit" tooltip="{i18n>Editcolorattributes}" press="setColorEditMode" visible="{= ${ColorEditModeModel>/editMode} === true ? false : true }" />
            <Button type="Ghost" icon="sap-icon://delete" tooltip="{i18n>Deleteselectedcolors}" press="onDeleteColor" visible="{= ${ColorEditModeModel>/editMode} === true ? false : true }" />
            <Button type="Ghost" icon="sap-icon://decline" tooltip="{i18n>Canceledit}" press="cancelColorsEdit" visible="{= ${ColorEditModeModel>/editMode} === true ? true : false }" />
        </Toolbar>

        <MessageStrip id="ColorsMessageStrip" text="" showIcon="true" showCloseButton="true" class="sapUiMediumMarginBottom" type="Error" visible="false" />

        <!-- Color Attributes Table -->
        <!-- <ScrollContainer height="100%" vertical="true"> {= ${ColorEditModeModel>/editMode} === true ? 'None' : 'MultiToggle' }-->
        <c:DynamicTable id="colorsTable" rows="{DataModel>/results}" enableColumnReordering="false" visibleRowCountMode="Auto" class="sapUiSizeCompact" selectionMode="MultiToggle">
            <c:columns>
                <t:Column name="ATTRIBSEQ" width="5rem" filterProperty="ATTRIBSEQ">
                    <Label text="{i18n>Seq}" />
                    <t:template>
                        <Text text="{DataModel>Attribseq}" />
                    </t:template>
                </t:Column>
                <t:Column width="11rem" filterProperty="Attribcd">
                    <Label text="{i18n>ColorCode}" />
                    <t:template>
                        <!-- <HBox class="sapUiResponsiveContentPadding">
                                    <Label required="true"/> -->
                        <Input id="ColorIdInput" value="{DataModel>Attribcd}" editable="false" valueStateText="Required" change="onColorChange" liveChange="onGeneralAttrChange" />
                        <!-- </HBox>                         -->
                    </t:template>
                </t:Column>
                <t:Column width="11rem" filterProperty="Desc1">
                    <Label text="{i18n>Description}" />
                    <t:template>
                        <Input value="{DataModel>Desc1}" editable="false" change="onColorChange" liveChange="onColorChange" />
                    </t:template>
                </t:Column>
            </c:columns>
            <c:layoutData>
                <FlexItemData growFactor="1" baseSize="0%" />
            </c:layoutData>
        </c:DynamicTable>
        <!-- </ScrollContainer> -->
    </VBox>

    <!-- </Panel> -->
</core:FragmentDefinition>
